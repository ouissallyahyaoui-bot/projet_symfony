{% extends 'base.html.twig' %}

{% block title %}{{ product.name }} - Our Store{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row">
        <div class="col-md-6">
            <span class="badge bg-primary mb-3">{{ product.category.name }}</span>
            <h1 class="mb-3">{{ product.name }}</h1>
            <h3 class="text-success mb-4">${{ product.price }}</h3>
            <p class="text-muted">{{ product.description }}</p>
            <a href="{{ path('cart_add', {id: product.id}) }}" class="btn btn-primary btn-lg">Add to Cart</a>
            <a href="{{ path('app_home') }}" class="btn btn-outline-secondary btn-lg">Continue Shopping</a>
        </div>
    </div>
</div>
{% endblock %}